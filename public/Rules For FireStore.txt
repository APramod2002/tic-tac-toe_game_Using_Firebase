rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /tictactoe_game/{docId} {
    	allow create: if request.auth != null;
      allow read, delete, update: if false;
    }
  }
}